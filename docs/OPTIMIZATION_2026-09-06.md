# HA-Seoul-Bike 1.3.2 — 최적화

2026-09-06 / Home Assistant 통합 패치

## 변경 내용

- 자동 재로그인 뒤 options의 이전 쿠키가 새 data 쿠키를 덮는 우선순위 문제 수정
- 인근 대여소 상위 N개 선택에서 전체 정렬을 부분 선택으로 교체
- 기존 자전거 수 우선·거리순·동률 입력순 및 총합·설정·폴링 주기는 유지

## 호환성과 범위

도메인, 기존 설정, 엔티티·장치 식별자와 공개 서비스 이름은 유지합니다. 의존성 일괄 업그레이드와 서버 측 세션 만료 우회는 포함하지 않습니다. SmartThings 브리지 앱과 ANCS 펌웨어는 변경하지 않습니다.

## 검증 범위

원본 전체 파일 해시를 확인한 뒤 기존 저장소 테스트를 변경 전후 실행합니다. HACS/Hassfest와 저장소별 CI 검증을 통과한 경우에만 릴리스를 게시합니다. 앞선 후보 묶음의 오프라인 테스트 103개와 실제 저장소 테스트 결과는 별개입니다. 운영 HA의 실제 로그인·쿠키 수명·재생·실기기 제어·장시간 운용은 검증하지 않았습니다. 테스트 통과만으로 모든 환경에서 무영향이나 성능 향상을 보장하지 않습니다.

## 업데이트와 되돌리기

HA 백업을 확보한 후 HACS에서 1.3.2을 내려받고 Home Assistant를 재시작합니다. 문제가 생기면 HACS 재다운로드에서 이전 버전 1.3.1을 선택하거나 백업을 복원합니다. 이 GitHub 배포 작업은 사용자 HA에 직접 설치하거나 재시작하지 않습니다.

기준 커밋: `96b743029a2ddd33916a2ca72383e25d5d73aa7d`

배포 검증 실행: https://github.com/1bobby-git/HA-Seoul-Bike/actions/runs/33998865132

## 실제 CI 결과

기존 코드: {'tests': 17, 'failures': 0, 'errors': 0, 'skipped': 0}

패치 코드: {'tests': 17, 'failures': 0, 'errors': 0, 'skipped': 0}

HACS와 Hassfest 검증을 통과한 뒤 게시합니다.
